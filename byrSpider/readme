简易的byr论坛爬虫

Purpose:
搜索byr论坛某一板块的标题信息中是否带有查询关键词，如有，则记下标题和url，存入结果文件。

Usage:
在InputTools.java中的initKeyWords(),initPageRange(),initBoard()方法中分别设置搜索的关键词（可以有若干个）、搜索页数、搜索板块。然后运行程序，程序会在子目录下生成以运行程序时的系统时间为名的文件，存放满足条件的项。

TO DO:
1.添加搜索的定制，可以自定义搜索板块，关键词，页数
2.添加数据库支持
3.实现论坛的全局搜索，不再局限在一个板块当中
